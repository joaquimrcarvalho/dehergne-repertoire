### Timeline

| Date       | Event                   |
|------------|-------------------------|
{% for date, bioitems in model.dated_bio()|dictsort -%}
    {% for a_bioitem in bioitems -%}
| {% if loop.first -%}{{ "No date" if date == "0" else util.format_timelink_date(date) }}{% else -%}  {% endif -%}
| {% include "markdown/base/entity_timeline_bioitem.j2" %}|
{% endfor -%}
{% endfor -%}
